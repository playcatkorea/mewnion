# 🔐 Google OAuth 설정 완벽 가이드

> 화면을 보면서 따라하세요! 모든 클릭 위치와 메뉴 이름을 정확하게 안내합니다.

---

## 📋 준비물

- Google 계정
- Supabase 프로젝트의 URL (예: `https://abcdefg.supabase.co`)

---

## 1️⃣ Google Cloud Console 프로젝트 만들기

### 1-1. Google Cloud Console 접속

```
https://console.cloud.google.com
```

위 주소로 접속 → Google 계정으로 로그인

---

### 1-2. 프로젝트 선택 드롭다운 찾기

화면 **맨 위 왼쪽**을 보세요:

```
┌────────────────────────────────────┐
│ ☰ Google Cloud    [프로젝트 선택 ▼] │
│                    ↑ 여기 클릭!      │
└────────────────────────────────────┘
```

**"프로젝트 선택"** (또는 "Select a project") 클릭

---

### 1-3. 새 프로젝트 만들기

팝업 창이 나타납니다:

```
┌──────────────────────────────────┐
│ 프로젝트 선택                      │
│ ────────────────────────────────  │
│ 🔍 프로젝트 검색                   │
│                                   │
│ [새 프로젝트]  ← 여기 클릭!        │
│                                   │
│ My First Project                  │
│ ...                               │
└──────────────────────────────────┘
```

**"새 프로젝트"** (또는 "NEW PROJECT") 버튼 클릭

---

### 1-4. 프로젝트 정보 입력

다음 화면에서:

```
프로젝트 이름: Mewnion
위치: 조직 없음 (No organization)
```

입력 후 **"만들기"** (또는 "CREATE") 버튼 클릭

⏳ 약 10초 대기 → "프로젝트가 생성되었습니다" 알림 확인

---

## 2️⃣ OAuth 동의 화면 설정

### 2-1. 좌측 메뉴 열기

화면 **왼쪽 위**의 햄버거 메뉴(☰) 클릭

```
┌─────────────────────────────┐
│ ☰  ← 이 아이콘 클릭          │
│                              │
│ 메뉴가 나타남 ↓              │
│ • IAM 및 관리자              │
│ • API 및 서비스  ← 찾기!     │
│ • Compute Engine             │
│ • ...                        │
└─────────────────────────────┘
```

---

### 2-2. OAuth 동의 화면 메뉴 찾기

좌측 메뉴에서:

1. **"API 및 서비스"** 찾기 (메뉴가 한글이면 정확히 이 이름)
2. 마우스를 올리면 하위 메뉴가 나타남
3. **"OAuth 동의 화면"** 클릭 (한글 메뉴에서 정확히 이 이름)

```
API 및 서비스
  ├─ 사용자 인증 정보
  ├─ OAuth 동의 화면  ← 여기 클릭!
  ├─ 라이브러리
  ├─ API 대시보드
  └─ ...
```

**💡 영어 메뉴인 경우:**
- "APIs & Services" → "OAuth consent screen"

---

### 2-3. User Type 선택

화면 중앙에 카드 2개가 나타납니다:

```
┌──────────────┐  ┌──────────────┐
│   Internal   │  │   External   │
│  (내부 전용)  │  │  (외부 사용자) │
│              │  │   ← 선택!     │
└──────────────┘  └──────────────┘
```

**"External"** 선택 → **"만들기"** (또는 "CREATE") 버튼 클릭

---

### 2-4. OAuth 동의 화면 정보 입력

#### 첫 번째 페이지: 앱 정보

다음만 입력하면 됩니다:

```
앱 이름: Mewnion
사용자 지원 이메일: 본인의 Gmail 주소 (드롭다운에서 선택)
```

**맨 아래로 스크롤**해서:

```
개발자 연락처 정보
이메일 주소: 본인의 Gmail 주소 입력
```

입력 완료 → **"저장 후 계속"** (또는 "SAVE AND CONTINUE") 클릭

---

#### 두 번째 페이지: Scopes

아무것도 입력하지 않고:

**"저장 후 계속"** (또는 "SAVE AND CONTINUE") 클릭

---

#### 세 번째 페이지: Test users (선택사항)

개발 중이라면 테스트 유저 추가 가능 (생략 가능)

**"저장 후 계속"** 클릭

---

#### 네 번째 페이지: 요약

설정 내용 확인 후:

**"대시보드로 돌아가기"** (또는 "BACK TO DASHBOARD") 클릭

✅ OAuth 동의 화면 설정 완료!

---

## 3️⃣ OAuth 클라이언트 ID 만들기

### 3-1. 사용자 인증 정보 메뉴로 이동

좌측 메뉴에서:

```
API 및 서비스
  ├─ 사용자 인증 정보  ← 여기 클릭!
  ├─ OAuth 동의 화면
  ├─ 라이브러리
  └─ ...
```

**"사용자 인증 정보"** 클릭 (한글 메뉴에서 정확히 이 이름)

**💡 영어 메뉴인 경우:** "Credentials"

---

### 3-2. 인증 정보 만들기

화면 **상단 중앙**에서:

```
┌──────────────────────────────────┐
│ + 사용자 인증 정보 만들기 ▼       │
│   ↑ 이 버튼 클릭!                 │
└──────────────────────────────────┘
```

**"+ 사용자 인증 정보 만들기"** 버튼 클릭 (한글 메뉴)

**💡 영어 메뉴:** "+ CREATE CREDENTIALS"

드롭다운 메뉴가 나타남:

```
• API 키
• OAuth 클라이언트 ID  ← 이거 선택!
• 서비스 계정
```

**"OAuth 클라이언트 ID"** 클릭 (한글 메뉴에서 정확히 이 이름)

**💡 영어 메뉴:** "OAuth client ID"

---

### 3-3. 애플리케이션 유형 선택

화면에 **"애플리케이션 유형"** 드롭다운이 나타납니다:

```
애플리케이션 유형:  [선택 ▼]

클릭하면:
○ 웹 애플리케이션      ← 이거 선택!
○ Android
○ Chrome 앱
○ iOS
○ 데스크톱 앱
```

**"웹 애플리케이션"** 선택 (한글 메뉴에서 정확히 이 이름)

**💡 영어 메뉴:** "Web application"

---

### 3-4. 상세 정보 입력

#### 이름 입력

```
이름: Mewnion Web App
```

---

#### 승인된 자바스크립트 원본 (Authorized JavaScript origins) 추가

**"+ URI 추가"** 버튼 클릭 (한글 메뉴에서는 이 텍스트)

**💡 영어 메뉴:** "+ ADD URI"

다음 URL들을 **하나씩** 추가:

```
http://localhost:5173
```

**"+ URI 추가"** 다시 클릭

```
https://mewnion.pages.dev
```

(커스텀 도메인이 있다면 추가로 입력)

---

---

#### ⚠️ 중요! 승인된 리디렉션 URI (Authorized redirect URIs) 추가

**"+ URI 추가"** 버튼 클릭 (**승인된 리디렉션 URI** 섹션에서)

**💡 영어 메뉴:** "+ ADD URI" in "Authorized redirect URIs" section

다음 URL 입력:

```
https://abcdefg.supabase.co/auth/v1/callback
```

⚠️ **주의**: `abcdefg` 부분을 **본인의 Supabase URL**로 교체하세요!

**본인의 Supabase URL 확인 방법:**
1. Supabase 대시보드 → Settings → API
2. "URL" 항목 복사 (예: `https://abcdefg.supabase.co`)
3. 뒤에 `/auth/v1/callback` 추가

**최종 형태:**
```
https://본인의프로젝트ID.supabase.co/auth/v1/callback
```

---

### 3-5. 만들기

모든 정보 입력 완료 후:

**"만들기"** 버튼 클릭 (화면 맨 아래)

**💡 영어 메뉴:** "CREATE" 버튼

---

### 3-6. 클라이언트 ID와 Secret 복사

팝업 창이 나타납니다:

```
┌────────────────────────────────────┐
│ OAuth 클라이언트 생성됨             │
│ (또는 "OAuth client created")      │
│ ────────────────────────────────   │
│                                     │
│ 클라이언트 ID:                      │
│ (Your Client ID)                   │
│ 123456789-abc.apps.googleusercontent.com │
│                      [복사] ← 클릭! │
│                                     │
│ 클라이언트 보안 비밀번호:           │
│ (Your Client secret)               │
│ GOCSPX-abcdefg123456               │
│                      [복사] ← 클릭! │
│                                     │
│            [확인]                   │
└────────────────────────────────────┘
```

**💡 한글 메뉴:**
- "클라이언트 ID" / "클라이언트 보안 비밀번호"

**💡 영어 메뉴:**
- "Your Client ID" / "Your Client secret"

**중요!** 다음 2개를 메모장에 복사해두세요:
- ✅ 클라이언트 ID (Client ID)
- ✅ 클라이언트 보안 비밀번호 (Client Secret)

복사 완료 → **"확인"** 버튼 클릭

**💡 영어 메뉴:** "OK" 버튼

✅ Google Cloud Console 설정 완료!

---

## 4️⃣ Supabase에 Google OAuth 연결

### 4-1. Supabase Authentication 페이지 접속

1. Supabase 대시보드 접속 (https://supabase.com)
2. 본인의 프로젝트 선택
3. 왼쪽 메뉴에서 **"🔐 Authentication"** 클릭

---

### 4-2. Providers 탭 선택

상단 탭 메뉴:

```
Users | Policies | Providers | Email Templates | ...
                    ↑ 여기 클릭!
```

**"Providers"** 탭 클릭

---

### 4-3. Google Provider 찾기

Provider 목록을 아래로 스크롤:

```
Auth Providers

Email        [활성화됨]
Phone        [비활성화]
...
Google       [비활성화] ← 찾기!
GitHub       [비활성화]
...
```

**"Google"** 행의 오른쪽 **펼치기 아이콘(>)** 클릭

---

### 4-4. Google Provider 설정

펼쳐진 섹션에서:

#### 1. Enabled 토글 켜기

```
┌────────────────────────────┐
│ Google enabled             │
│ ───────────── [○──] ← OFF  │
│               [──●] ← ON   │
└────────────────────────────┘
```

토글을 **ON** 위치로 변경

---

#### 2. Client ID 입력

```
Client ID (for OAuth)
┌─────────────────────────────────────┐
│ 123456789-abc.apps.googleusercontent.com │
└─────────────────────────────────────┘
```

Google Cloud Console에서 복사한 **Client ID** 붙여넣기

---

#### 3. Client Secret 입력

```
Client Secret (for OAuth)
┌──────────────────────────┐
│ GOCSPX-abcdefg123456     │
└──────────────────────────┘
```

Google Cloud Console에서 복사한 **Client Secret** 붙여넣기

---

#### 4. Callback URL 확인

설정 화면 아래쪽에 다음이 표시됩니다:

```
Callback URL (for OAuth)

https://abcdefg.supabase.co/auth/v1/callback
                           [복사]
```

이 URL이 **Google Cloud Console의 Authorized redirect URIs**에 추가되어 있는지 확인!

만약 다르다면:
1. Google Cloud Console로 돌아가기
2. Credentials → OAuth 2.0 Client IDs → 본인이 만든 클라이언트 클릭
3. Authorized redirect URIs에 이 URL 추가
4. 저장

---

### 4-5. 저장

모든 입력 완료 후:

화면 맨 아래의 **"Save"** 버튼 클릭

✅ 성공 메시지 확인!

---

## 5️⃣ Supabase Redirect URL 설정

### 5-1. URL Configuration 메뉴

Supabase 대시보드에서:

```
Authentication
  ├─ Users
  ├─ Policies
  ├─ Providers
  └─ URL Configuration  ← 여기 클릭!
```

---

### 5-2. Site URL 설정

```
Site URL
┌────────────────────────────┐
│ https://mewnion.pages.dev  │
└────────────────────────────┘
```

본인의 프로덕션 도메인 입력

---

### 5-3. Redirect URLs 추가

```
Redirect URLs
┌─────────────────────────────────┐
│ http://localhost:5173/**        │
│ https://mewnion.pages.dev/**    │
└─────────────────────────────────┘
```

**각 URL 끝에 `/**` 붙이기!**

입력 완료 → **"Save"** 클릭

---

## ✅ 완료 체크리스트

설정이 제대로 되었는지 확인:

### Google Cloud Console
- [ ] 프로젝트 생성 (`Mewnion`)
- [ ] OAuth 동의 화면 설정 (External)
- [ ] OAuth 클라이언트 ID 생성
- [ ] Authorized JavaScript origins: `http://localhost:5173`, `https://mewnion.pages.dev`
- [ ] **Authorized redirect URIs: `https://본인ID.supabase.co/auth/v1/callback`**
- [ ] Client ID와 Secret 복사

### Supabase
- [ ] Authentication → Providers → Google 활성화
- [ ] Client ID 입력
- [ ] Client Secret 입력
- [ ] Callback URL 확인
- [ ] URL Configuration 설정
- [ ] Site URL 입력
- [ ] Redirect URLs 입력 (끝에 `/**`)

---

## 🧪 테스트

```bash
npm run dev
```

http://localhost:5173/signup 접속

**"Google로 회원가입"** 버튼 클릭 → Google 로그인 창이 뜨면 성공! 🎉

---

## 🐛 자주 발생하는 에러

### Error 400: redirect_uri_mismatch

**원인**: Redirect URI가 정확하게 일치하지 않음

**해결**:
1. Supabase → Authentication → Providers → Google 섹션 열기
2. "Callback URL" 복사 (예: `https://abc123.supabase.co/auth/v1/callback`)
3. Google Cloud Console → Credentials로 이동
4. OAuth 2.0 Client ID 클릭
5. Authorized redirect URIs 섹션 확인
6. 위에서 복사한 URL이 **정확히** 있는지 확인
7. 없다면 추가 → 저장
8. 5분 대기 (Google 설정 반영 시간)

---

### Error 401: invalid_client

**원인**: Client ID 또는 Secret이 잘못됨

**해결**:
1. Google Cloud Console → Credentials
2. OAuth 2.0 Client ID 클릭
3. Client ID와 Client Secret 다시 복사
4. Supabase → Authentication → Providers → Google
5. 다시 붙여넣기 → Save

---

### "popup_closed_by_user"

**원인**: 사용자가 팝업을 닫음 (에러 아님)

**해결**: 다시 시도

---

완료! 🎉 이제 Google 로그인이 작동합니다!
